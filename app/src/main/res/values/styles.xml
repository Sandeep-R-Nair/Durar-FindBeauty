<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.BottomNavigationView"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/opensans_semi_bold</item>
    </style>

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <!-- Customize text appearance -->
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/quicksand_regular</item>

        <!-- Customize hint appearance -->
        <item name="hintTextAppearance">@style/CustomSearchViewHint</item>
    </style>

    <style name="CustomSearchViewHint" parent="TextAppearance.AppCompat">
        <item name="android:textSize">16sp</item>
        <item name="android:textColorHint">#565656</item>
        <!-- Add more hint-related attributes as needed -->
    </style>

</resources>